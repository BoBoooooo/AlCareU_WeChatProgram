<view class="page">
     <view class="no-order" hidden="{{orderList ? true : false}}">
        <image src="/images/no-order.png" class="no-order-img"></image>
        <view class="text">暂无体检报告</view>
    </view>
    <!-- <view class="page__bd">
      
        <view class="weui-cells weui-cells_after-title"  >
            <view  class="weui-cell weui-cell_access" hover-class="weui-cell_active" bindtap="orderDetail" data-id="{{item.ID}}" wx:for-items="{{orderList}}" wx:key="{{index}}" >
                <view class="weui-cell__bd">{{item.examstime}}  {{item.ReportName}}</view>
                <view class="weui-cell__ft weui-cell__ft_in-access"></view>
            </view>
        
        </view>

    </view> -->
    <view class="order-list" hidden="{{orderList ? false : true}}">
        <view class="a-order" wx:for-items="{{orderList}}" wx:key="{{index}}" style='position:relative'>
            <view class="order-date">
                <view class="date-box">体检时间：{{item.examstime}}</view>
                <!-- <view class="status {{(item.status==-1 || item.status==4) ? '':'red'}}">{{item.statusStr}}</view> -->
            </view>
             <view class="order-date">
                <view class="date-box">报告名称：{{item.ReportName}}</view>
                <!-- <view class="status {{(item.status==-1 || item.status==4) ? '':'red'}}">{{item.statusStr}}</view> -->
            </view>
          
            <image src='/images/accessory.png' class="weui-btn mini-btn" type="default" size="mini"  data-id="{{item.ID}}" bindtap="orderDetail" style='position:absolute;right:30rpx;top:30rpx;width:50rpx;height:50rpx'></image>
        </view>

    </view>
</view>